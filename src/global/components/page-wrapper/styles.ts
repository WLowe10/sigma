import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
    pageWrapper: {
        overflow: "auto",
        position: "absolute",
        top: 0, 
        left: 0,
        right: 0,
        bottom: 0,
        padding: "2rem",
        // maxWidth: "60rem",
    }
}); 